<!DOCTYPE html>
<html>
<head>
    <title>Card</title>
</head>
<body>
    <div class="card-view-container">
    <header class="page-header">
        <a href="/dashboard" class="header-button back-button">
           &larr; Home
        </a>
        <h3 id="transfers-title">MY CARD</h3>
    </header>
    <div class="card-container">
        <div class="card-face">
            <div class="card-number">
                <span>Card Number</span>
                <div><%= @card.number.scan(/.{4}/).join(' ') %></div>
            </div>
            <div class="card-details">
                <div class="card-holder">
                    <span>Card Owner</span>
                    <div><%= @user.name.to_s.upcase %></div>
                </div>
                 <div class="card-cvv">
                        <span>CVV</span>
                        <div><%= @card.cvv %></div>
                    </div>
                <div class="card-expiry">
                    <span>Expires</span>
                    <div><%= @card.expiry_date.strftime('%m/%y') %></div>
                </div>
            </div>
        </div>
    </div>
</div>
</body>
</html>